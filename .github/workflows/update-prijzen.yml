name: Brandstofprijzen bijwerken
 
on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
 
jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
 
    steps:
      - name: Code ophalen
        uses: actions/checkout@v4
 
      - name: Python instellen
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
 
      - name: Pakketten installeren
        run: pip install requests beautifulsoup4
 
      - name: Tankstations ophalen
        run: python haal_tankstations_op.py
 
      - name: Prijzen scrapen
        run: python scraper.py
 
      - name: Opslaan in repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add prijzen.json tankstations.json
          git diff --staged --quiet || git commit -m "Prijzen bijgewerkt $(date +'%Y-%m-%d')"
          git push
